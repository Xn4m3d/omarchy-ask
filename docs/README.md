# docs

Screenshots and short screen recordings for the main README go here.

Planned, one per feature so each can be linked on its own:

| File | Shows |
|---|---|
| `card.png` | the composer with context chips |
| `streaming.gif` | an answer arriving, activity dot and stream cursor |
| `attachments.gif` | `ctrl+r` region capture becoming a chip |
| `picker.png` | the file picker, filtered |
| `history.png` | past turns with session ids |
| `settings.png` | the settings screen |
| `themes.png` | the same card under three Omarchy themes |

Capture them on an empty workspace: the card is a layer-shell surface over a
dimmed screen, so whatever is behind it ends up in the shot.
